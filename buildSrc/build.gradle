plugins {
    id 'java-gradle-plugin'
}

gradlePlugin {
    plugins {
        simplePlugin {
            id = 'com.mflyyou.greeting'
            implementationClass = 'com.mflyyou.plugin.GreetingPlugin'
        }
    }
}